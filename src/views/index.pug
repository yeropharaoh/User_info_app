doctype html
html
	head
		script(src='http://ajax.googleapis.com/ajax/libs/jquery/2.1.1/jquery.min.js')
	body
		h1 All the Users info is displayed below
		//- p= Users
		each user in Users
			li This user's first name is #{user.firstname}
			li This user's last name is #{user.lastname}
			li This user's email is #{user.email}
	br
			
	a(href='/search') SEARCH
	|
	|
	
	br
	br
		
	a(href='/form') SUBSCRIPTION FORM
	|
	|

	script.
			$("#goButton").click(function() {
				var fileData = {
					name: $("#filename").val(),
					contents: $("#fileContents").val()
				};
